# 최근 열람 파일 사이드바 추가 (2026-03-05)

## 변경 배경
- 반복적으로 여는 파일에 빠르게 접근할 수 있는 UI가 없었습니다.
- 매번 트리 탐색을 다시 해야 해 작업 전환 비용이 발생했습니다.

## 적용 내용
- `web/static/index.html`
  - 사이드바에 `최근 열람` 섹션 추가 (`recent-files-list`)
- `web/static/app.js`
  - localStorage 키 추가: `RECENT_FILES_STORAGE_KEY`
  - 최근 파일 관리 함수 추가
    - `initRecentFiles()`
    - `loadRecentFilesFromStorage()`
    - `saveRecentFilesToStorage()`
    - `renderRecentFiles()`
    - `addRecentFile()`
  - 파일 열기 성공 시 최근 목록 자동 갱신
- `web/static/style.css`
  - 최근 열람 카드형 목록 스타일 추가

## 기대 효과
- 자주 보는 문서로 빠르게 이동할 수 있어 탐색 시간이 줄어듭니다.
- 최근 작업 컨텍스트가 유지되어 연속 작업 효율이 향상됩니다.

## 테스트
- 실행 명령: `.venv/bin/python -m pytest -q`
- 결과: `74 passed`
