# 업로드 폴더 피커 트리 탐색 개선 (2026-03-05)

## 변경 배경
- 업로드 대상 폴더 선택 UI가 사실상 평면 목록이라 깊은 하위 폴더 탐색이 불편했습니다.
- 트리 구조를 직접 확인하며 선택할 수 있는 탐색 UI가 필요했습니다.

## 적용 내용
- `web/static/app.js`
  - 업로드 피커 펼침 상태(`uploadPickerExpanded`) 상태 추가
  - 렌더 함수 개편
    - `buildFolderPicker()`
    - `renderUploadFolderNode()`
  - 노드별 화살표 클릭으로 폴더 접기/펼치기 지원
  - 폴더 선택 시 트리 다시 렌더링하여 선택 상태를 일관되게 반영
- `web/static/style.css`
  - 트리 피커 전용 스타일 추가
    - `.folder-tree-item`
    - `.folder-tree-arrow`
    - `.folder-tree-label`
    - `.folder-tree-children`

## 기대 효과
- 깊은 폴더 구조에서도 업로드 경로를 빠르게 탐색하고 선택할 수 있습니다.
- 선택 상태와 펼침 상태가 시각적으로 명확해져 실수 선택이 줄어듭니다.

## 테스트
- 실행 명령: `.venv/bin/python -m pytest -q`
- 결과: `68 passed`
