# 원본/AI 결과 분할 비교 뷰 (2026-03-05)

## 변경 배경
- AI 결과를 검토할 때 원본과 결과를 번갈아 확인해야 해 비교 효율이 낮았습니다.
- 동일 화면에서 원문 대비 결과를 나란히 확인하는 UI가 필요했습니다.

## 적용 내용
- `web/static/index.html`
  - AI 패널 헤더에 `분할뷰` 버튼(`split-view-btn`) 추가
- `web/static/app.js`
  - 상태값 `splitViewEnabled` 추가
  - 분할뷰 로직 추가
    - `updateSplitViewButton()`
    - `renderSplitComparison()`
    - `toggleSplitView()`
  - AI 결과 렌더 경로(`renderAIResult`)와 초기화(`resetAIResult`)를 분할뷰 모드에 맞게 분기
- `web/static/style.css`
  - 2열 비교 레이아웃(`.split-compare`, `.split-pane`) 추가
  - 모바일에서 단일 열로 자동 전환

## 기대 효과
- 원본과 AI 결과를 동시에 보며 정확도/누락 여부를 빠르게 검토할 수 있습니다.
- 리뷰 흐름이 단순해져 결과 확인 시간이 단축됩니다.

## 테스트
- 실행 명령: `.venv/bin/python -m pytest -q`
- 결과: `82 passed`
